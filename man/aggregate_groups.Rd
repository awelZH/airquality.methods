% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{aggregate_groups}
\alias{aggregate_groups}
\title{Summarise a value column per group}
\usage{
aggregate_groups(
  data,
  y,
  groups,
  nmin = 3,
  perc = list(ymin = 0.05, lower = 0.25, middle = 0.5, upper = 0.75, ymax = 0.95)
)
}
\arguments{
\item{data}{A data frame in long format.}

\item{y}{Name of the value column, as a string.}

\item{groups}{Names of the grouping columns, as a character vector.}

\item{nmin}{Smallest group size for which statistics are reported.}

\item{perc}{Percentiles used for \code{minlower}, \code{lower}, \code{middle}, \code{upper} and
\code{maxupper}.}
}
\value{
A tibble with one row per group combination.
}
\description{
Produces the set of statistics the air quality figures draw from: counts,
percentiles for box and ribbon plots, mean, spread and sum.
}
\details{
Two deliberate properties:
\itemize{
\item Group combinations that do not occur in the data are added with \code{n = 0} and
\code{NA} statistics, so a gap stays visible in a figure instead of silently
closing.
\item Groups with fewer than \code{nmin} observations get \code{NA} statistics, because a
percentile of two values is not a percentile.
}
}
\examples{
data <- data.frame(site = rep(c("A", "B"), each = 5), value = 1:10)
aggregate_groups(data, "value", "site")

}
