% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-align.R
\name{align_to_grid}
\alias{align_to_grid}
\title{Normalise every collection onto a freely chosen grid}
\usage{
align_to_grid(
  x,
  grid,
  years = NULL,
  methods = resampling_methods(),
  default_method = "average",
  year_match = "exact",
  max_year_diff = Inf,
  no_data_value = -9999
)
}
\arguments{
\item{x}{Tibble from \code{\link[=read_geo_admin]{read_geo_admin()}}.}

\item{grid}{Target raster: a \code{stars} object, the path/url of a raster file, or
\code{\link[=make_reference_grid]{make_reference_grid()}}. Only the geometry is used.}

\item{years}{Target years (\code{NULL} = every year present in \code{x}). For a gapless
annual series with interpolation, name the years explicitly.}

\item{methods}{Named vector, collection id to GDAL method (by default from
\code{\link[=geo_admin_specs]{geo_admin_specs()}}: STATPOP \code{"sum"}, everything else \code{"average"}).}

\item{default_method}{Method for collections not named in \code{methods}.}

\item{year_match}{Temporal matching. \code{"exact"} (the default) uses data of the
same year only. Optionally one value for every collection (\code{"nearest"},
\code{"linear"}) or a named vector; collections not named stay \code{"exact"}. See
\code{\link[=select_year_sources]{select_year_sources()}}.}

\item{max_year_diff}{Largest accepted distance in years for \code{"nearest"} and
\code{"linear"}.}

\item{no_data_value}{See \code{\link[=resample_to_grid]{resample_to_grid()}}.}
}
\value{
A tibble with one row per year: \code{year}, \code{res_x}, \code{res_y}, \code{grid},
\code{stars}, \code{sources} and the noloc columns.
}
\description{
Each collection is brought onto \code{grid} per year with GDAL, finer or coarser:
summed (STATPOP, the total is preserved) or averaged (everything else),
according to \code{methods}. Since all years share one grid, \code{\link[=stack_years]{stack_years()}} on the
result yields exactly one cube.
}
\details{
The \code{total_source} and \code{total_aligned} columns of \code{sources} show the totals
before and after resampling under \code{"sum"}. They differ only when the grid is
smaller than the data that was read in. The noloc columns refer to the extent
used when reading.
}
