% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-align.R
\name{align_to_reference}
\alias{align_to_reference}
\title{Align collections onto the grid of a reference collection, year by year}
\usage{
align_to_reference(
  x,
  reference = "ch.bfs.statistik-bevoelkerung_haushalte",
  methods = resampling_methods(),
  default_method = "average",
  year_match = "exact",
  max_year_diff = Inf,
  no_data_value = -9999
)
}
\arguments{
\item{x}{Tibble from \code{\link[=read_geo_admin]{read_geo_admin()}} holding at least two collections.}

\item{reference}{Collection id of the reference (STATPOP by default).}

\item{methods}{Named vector, collection id to GDAL method (by default from
\code{\link[=geo_admin_specs]{geo_admin_specs()}}: STATPOP \code{"sum"}, everything else \code{"average"}).}

\item{default_method}{Method for collections not named in \code{methods}.}

\item{year_match}{Temporal matching. \code{"exact"} (the default) uses data of the
same year only. Optionally one value for every collection (\code{"nearest"},
\code{"linear"}) or a named vector; collections not named stay \code{"exact"}. See
\code{\link[=select_year_sources]{select_year_sources()}}.}

\item{max_year_diff}{Largest accepted distance in years for \code{"nearest"} and
\code{"linear"}.}

\item{no_data_value}{See \code{\link[=resample_to_grid]{resample_to_grid()}}.}
}
\value{
A tibble with one row per reference year: \code{year}, \code{reference},
\code{res_x}, \code{res_y}, \code{grid}, \code{stars}, \code{sources} (the per-attribute log) and the
noloc columns.
}
\description{
For every year of the reference collection its own raster is used as the
template, so the reference resolution may change between years. The reference
itself stays untouched; every other collection is brought onto that grid with
GDAL, averaged or summed according to \code{methods}.
}
