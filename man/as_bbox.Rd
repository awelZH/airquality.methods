% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-raster.R
\name{as_bbox}
\alias{as_bbox}
\title{Normalise a bounding box}
\usage{
as_bbox(bbox, crs = 2056, call = rlang::caller_env())
}
\arguments{
\item{bbox}{\code{NULL}, a numeric vector \code{c(xmin, ymin, xmax, ymax)} in \code{crs}
(LV95 by default), or an object carrying its own crs (\code{sf}, \code{sfc}, \code{bbox},
\code{stars}), which is transformed to \code{crs} when needed.}

\item{crs}{Target coordinate reference system (EPSG code or \code{\link[sf:st_crs]{sf::st_crs()}}).}

\item{call}{Environment used for the error call.}
}
\value{
An \code{\link[sf:st_bbox]{sf::st_bbox()}} object in \code{crs}, or \code{NULL} if \code{bbox} is \code{NULL}.
}
\description{
Accepts the several shapes an extent arrives in and always returns an
\code{\link[sf:st_bbox]{sf::st_bbox()}} object in \code{crs}.
}
\keyword{internal}
