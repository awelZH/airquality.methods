% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-cache.R
\name{asset_source}
\alias{asset_source}
\title{Where an asset will be read from}
\usage{
asset_source(asset, cache_dir)
}
\arguments{
\item{asset}{A single row from \code{\link[=get_geo_admin_assets]{get_geo_admin_assets()}}.}

\item{cache_dir}{Cache directory.}
}
\value{
\code{"stream"}, \code{"cache"} or \code{"download"}.
}
\description{
Uncompressed GeoTIFFs are streamed through \verb{/vsicurl/} and never cached (see
\code{\link[=read_tif_stars]{read_tif_stars()}}); everything else is read from the cache, after a download
if the file is not there yet. Only the existence of the cached file is
checked, not its checksum, so a corrupt entry reports \code{"cache"} and is then
downloaded again by \code{\link[=download_geo_admin_asset]{download_geo_admin_asset()}} with its own message.
}
\keyword{internal}
