% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/municipalities.R
\name{assign_municipalities}
\alias{assign_municipalities}
\title{Assign each cell the municipality its centre lies in}
\usage{
assign_municipalities(cells, map_municipalities)
}
\arguments{
\item{cells}{Tibble with cell centre coordinates \code{x}, \code{y}.}

\item{map_municipalities}{\code{sf} polygons with \code{bfs} and \code{gemeindename}.}
}
\value{
\code{cells} with \code{bfsnr} and \code{gemeindename} added (\code{NA} outside the
canton).
}
\description{
Exclaves are separate features with the same \code{bfs} number, so every cell is
matched to at most one feature and counted exactly once. Cells whose centre
lies in a lake belonging to no municipality (\code{bfs = 0}, no name) are
inhabited shore cells: they get the nearest municipality. Cells outside every
feature are outside the canton. Remove enclaves of other cantons first, see
\code{\link[=drop_foreign_enclaves]{drop_foreign_enclaves()}}.
}
