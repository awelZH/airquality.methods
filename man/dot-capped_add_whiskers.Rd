% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-capped-guide.R
\name{.capped_add_whiskers}
\alias{.capped_add_whiskers}
\title{Attach whisker grobs to the finished guide gtable}
\usage{
.capped_add_whiskers(gt, params, elements)
}
\description{
Called from \code{assemble_drawing()} — \code{params} (including the resolved
\code{direction}) and \code{elements} are already available there in the same form
the guide itself uses for labels and ticks. That keeps whisker labels
positioned exactly like the other labels.
}
\details{
Whiskers go into a newly inserted gtable row (vertical) or column
(horizontal); its size is \code{whisker_gap} plus half the label extent. The
cross-stroke is drawn in absolute units exactly \code{whisker_gap} from the bar
edge — the gap is thus independent of data range and bar size.
}
\keyword{internal}
