% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-capped-palette.R
\name{.capped_palette}
\alias{.capped_palette}
\title{Normalise a palette into a common form}
\usage{
.capped_palette(palette = "Magma", direction = 1, values = NULL, space = "Lab")
}
\arguments{
\item{palette}{One of:
\itemize{
\item a viridis map by name — "Magma" (the default), "Inferno", "Plasma",
"Viridis", "Cividis", "Rocket", "Mako", "Turbo" — or by the
single-letter \code{option} code viridis itself uses,
\item a string naming a palette from \code{grDevices::hcl.pals()}
(e.g. "YlOrRd", "Blue-Red 3", "Zissou 1"),
\item a vector of colours (e.g. \code{c("white", "steelblue", "black")}),
\item a function \code{f(n)} returning n colours (e.g. \code{scales::pal_viridis()},
\verb{\\(n) RColorBrewer::brewer.pal(n, "RdBu")}).
}}

\item{direction}{1 = as given, -1 = reversed.}

\item{values}{Optional positions of the palette colours in [0, 1] (as in
\verb{scale_*_gradientn(values = )}); allows non-linear colour ramps.}

\item{space}{Interpolation space, "Lab" (default) or "rgb".}
}
\value{
A list with
\itemize{
\item \code{n_pal(n)} — exactly n colours (for discrete/binned),
\item \code{ramp(x)}  — colour(s) for x in [0, 1] (for continuous).
}
}
\description{
Normalise a palette into a common form
}
\keyword{internal}
