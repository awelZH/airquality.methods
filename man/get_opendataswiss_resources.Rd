% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-tabular.R
\name{get_opendataswiss_resources}
\alias{get_opendataswiss_resources}
\title{List the resources of an opendata.swiss dataset}
\usage{
get_opendataswiss_resources(
  apiurl,
  useragent = "Amt für Abfall, Wasser, Energie und Luft, Kanton Zürich"
)
}
\arguments{
\item{apiurl}{Package-show url of the CKAN API.}

\item{useragent}{User agent sent with the request; opendata.swiss refuses requests without one.}
}
\value{
Tibble with one row per resource that has a download url, in the order of the API:
\code{download_url}, \code{format}, \code{modified} (character, as published) and \code{byte_size} (numeric); \code{NA}
where the API leaves a field out.
}
\description{
The version of each resource as the CKAN API describes it, so that a pipeline can check it on every
run and download the data only when it changed. opendata.swiss publishes no checksum (\code{hash} is
empty), so the modification time and the size stand for the version.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
api <- "https://ckan.opendata.swiss/api/3/action/package_show"
get_opendataswiss_resources(paste0(api, "?id=luftschadstoffemissionen-im-kanton-zurich"))
\dontshow{\}) # examplesIf}
}
