% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legend-grouped.R
\name{grouped_key}
\alias{grouped_key}
\title{Unique key per group and element, for a grouped legend}
\usage{
grouped_key(group, key, order = NULL, group_order = NULL, sep = "::")
}
\arguments{
\item{group}{Group of each element, e.g. the sector.}

\item{key}{Element, e.g. the subsector.}

\item{order}{Sort order of the elements (numeric); \code{NULL} keeps the order of appearance.}

\item{group_order}{Groups in the wanted order; groups not listed follow in their default order.}

\item{sep}{Separator between group and element; must not occur in the names.}
}
\value{
Factor with values \code{"<group><sep><key>"}.
}
\description{
Elements with the same name in different groups (e.g. "verschiedene" in several sectors) get
different keys. The factor levels define the order of the stack and of the legend: groups in
the order of their first element (or \code{group_order}), elements within a group by \code{order}.
}
\examples{
grouped_key(c("Verkehr", "Industrie", "Verkehr"), c("Strasse", "verschiedene", "verschiedene"))

}
