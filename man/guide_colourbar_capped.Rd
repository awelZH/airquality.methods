% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-capped-guide.R
\name{guide_colourbar_capped}
\alias{guide_colourbar_capped}
\alias{guide_coloursteps_capped}
\alias{guide_colorbar_capped}
\title{Colour bar guide with cap labels and whiskers}
\usage{
guide_colourbar_capped(
  ...,
  cap = TRUE,
  include_cap_val = TRUE,
  cap_labels = TRUE,
  cap_min_dist = NULL,
  whiskers = TRUE,
  whisker_gap = grid::unit(2, "mm"),
  whisker_avoid_overlap = TRUE,
  whisker_stem = TRUE,
  whisker_width = 1,
  whisker_colour = NULL,
  whisker_linewidth = NULL,
  whisker_linetype = 1,
  whisker_labels = TRUE,
  steps = FALSE
)

guide_coloursteps_capped(...)

guide_colorbar_capped(
  ...,
  cap = TRUE,
  include_cap_val = TRUE,
  cap_labels = TRUE,
  cap_min_dist = NULL,
  whiskers = TRUE,
  whisker_gap = grid::unit(2, "mm"),
  whisker_avoid_overlap = TRUE,
  whisker_stem = TRUE,
  whisker_width = 1,
  whisker_colour = NULL,
  whisker_linewidth = NULL,
  whisker_linetype = 1,
  whisker_labels = TRUE,
  steps = FALSE
)
}
\arguments{
\item{...}{Arguments for \code{\link[ggplot2:guide_colourbar]{ggplot2::guide_colourbar()}} /
\code{\link[ggplot2:guide_coloursteps]{ggplot2::guide_coloursteps()}} (e.g. \code{title}, \code{theme}, \code{reverse},
\code{barheight} via \code{theme}, \code{even.steps}, \code{show.limits}).}

\item{cap}{\code{TRUE}/\code{FALSE} — comparison-sign labelling of the scale ends.}

\item{include_cap_val}{Does the limit value itself belong to the edge
class? With \code{oob = scales::oob_squish} the answer is yes: the function
leaves the limit unchanged and pulls everything beyond it to that value.
The limit therefore gets the same colour as the capped values; for
binned scales it falls into the same class thanks to \code{include.lowest},
for both \code{right = TRUE} and \code{FALSE}. The edge class is thus
"x ≤ lim[1]" or "x ≥ lim[2]", and \code{TRUE} (default) labels
it accordingly with "≤" and "≥". \code{FALSE} selects the common
shorthand "<" and ">", which omits the limit value.}

\item{cap_labels}{Formatting of the cap value: \code{TRUE} (default) uses the
scale's \code{labels} function if set, and otherwise the same precision as
the other labels. Alternatively a function (e.g.
\code{scales::label_number(accuracy = 0.1)}).}

\item{cap_min_dist}{Minimum distance between a newly inserted edge break
and the existing breaks; closer breaks are dropped so labels don't
overlap. Default \code{NULL} = automatic, from the actual text extent and the
bar length. A number forces a fixed fraction of the bar length instead
(e.g. \code{0.05}), \code{0} disables thinning.}

\item{whiskers}{\code{TRUE}/\code{FALSE} — draw whiskers for the data extremes.}

\item{whisker_gap}{Fixed gap between the bar edge and the whisker (\code{unit}).
With \code{whisker_stem = TRUE} this is also the stem length.}

\item{whisker_avoid_overlap}{When \code{TRUE} (default), \code{whisker_gap} is only
enlarged if the whisker label would otherwise run into the label at the
bar end (e.g. ">100"). \code{FALSE} forces an exactly constant gap — then
\code{whisker_gap} must be chosen large enough.}

\item{whisker_stem}{\code{TRUE} (default) connects the bar edge and the
cross-stroke with a line, as in a boxplot. \code{FALSE} leaves the gap empty.}

\item{whisker_width}{Length of the cross-stroke relative to the bar width
(1 = as wide as the bar).}

\item{whisker_colour, whisker_linewidth, whisker_linetype}{Appearance of the
whisker. Default: legend text colour (\code{legend.text}) and the theme's
baseline weight (\code{line}, usually 0.5).}

\item{whisker_labels}{\code{TRUE} (labelled as described under \code{cap_labels}),
\code{FALSE} (whisker drawn, but unlabelled), or a function that formats the
value (e.g. \code{scales::label_number(accuracy = 0.1)}).}

\item{steps}{\code{TRUE} for binned scales (coloursteps), otherwise a colour
bar.}
}
\value{
A guide object.
}
\description{
Behaves like \code{\link[ggplot2:guide_colourbar]{ggplot2::guide_colourbar()}} or \code{\link[ggplot2:guide_coloursteps]{ggplot2::guide_coloursteps()}},
but additionally draws:
\itemize{
\item "≤"/"≥" on the lowest/highest label once data lie outside
\code{limits} (the break at the scale end is added if needed),
\item the true data minimum/maximum as a whisker at a fixed distance outside
the colour bar, with its own label. The whisker is built like a
boxplot's: a stem from the bar edge outward, and crossing it, the mark
for the extreme value.
}
}
\details{
Both only appear when the scale actually pulls out-of-range values to the
edge (\code{oob = scales::oob_squish}). With \code{oob = scales::oob_censor} or
\code{oob_keep} such values become \code{NA} — then there's no edge class to label,
and both the guide and the whisker are omitted.
}
