% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-raster.R
\name{local_quiet_vsicurl}
\alias{local_quiet_vsicurl}
\title{Stop GDAL probing for sidecar files over the network}
\usage{
local_quiet_vsicurl(extensions = ".tif", envir = rlang::caller_env())
}
\arguments{
\item{extensions}{File extensions \verb{/vsicurl/} is allowed to fetch.}

\item{envir}{Frame the reset is registered on.}
}
\value{
The previous value, invisibly.
}
\description{
Opening a remote raster, GDAL looks for companion files (\code{.aux.xml}, \code{.ovr},
\code{.msk}, ...). On data.geo.admin.ch none of them exist, so every probe costs an
HTTP request and raises a 404 warning — 39 of them for a single GeoTIFF.
Restricting the extensions \verb{/vsicurl/} may fetch suppresses the search.
Cloud-optimised GeoTIFFs carry their overviews inline, so nothing is lost.
}
\details{
The setting is restored when the calling function exits, so it cannot leak
into other \verb{/vsicurl/} use in the same session.
}
\keyword{internal}
