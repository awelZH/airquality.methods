% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-raster.R
\name{read_asset_stars}
\alias{read_asset_stars}
\title{Read one asset as a georeferenced stars object}
\usage{
read_asset_stars(
  asset,
  variables = NULL,
  bbox = NULL,
  coords = c("E_KOORD", "N_KOORD"),
  cellsize = 100,
  anchor = c("lowerleft", "center"),
  crs = 2056,
  cache_dir = geo_admin_cache_dir()
)
}
\arguments{
\item{asset}{A single row from \code{\link[=get_geo_admin_assets]{get_geo_admin_assets()}}.}

\item{variables}{Attributes or bands to keep (\code{NULL} = all). For large extents
select only what is needed: Switzerland at 100 m is ~8 million cells per
variable.}

\item{bbox}{Extent, numeric in LV95 or any \code{sf}/\code{bbox}/\code{stars} object.}

\item{coords, cellsize, anchor}{See \code{\link[=table_to_stars]{table_to_stars()}} (tabular formats only).}

\item{crs}{Expected coordinate reference system.}

\item{cache_dir}{Cache directory for downloads.}
}
\value{
A \code{stars} object, normalised so that grids are directly comparable.
}
\description{
GeoTIFFs are read directly; tabular assets (parquet, csv hectare grids) are
downloaded, cached and rasterised via \code{\link[=table_to_stars]{table_to_stars()}}.
}
