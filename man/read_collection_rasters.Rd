% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-collections.R
\name{read_collection_rasters}
\alias{read_collection_rasters}
\title{Read the raster data of one collection, year by year}
\usage{
read_collection_rasters(
  assets,
  years = NULL,
  bbox = bbox_zh_lv95,
  pattern = NULL,
  formats = c("tif", "parquet", "csv"),
  variables = NULL,
  name = NULL,
  epsg = 2056,
  cache_dir = geo_admin_cache_dir(),
  ...
)
}
\arguments{
\item{assets}{Tibble from \code{\link[=get_geo_admin_assets]{get_geo_admin_assets()}}.}

\item{years}{Wanted years (\code{NULL} = all available).}

\item{bbox}{Extent, by default the Canton of Zurich.}

\item{pattern, formats, epsg}{See \code{\link[=resolve_assets]{resolve_assets()}}.}

\item{variables}{Bands or table columns (\code{NULL} = all).}

\item{name}{Attribute name for single band rasters (by default derived from
the collection id).}

\item{cache_dir}{Cache directory.}

\item{...}{Further arguments passed to \code{\link[=read_asset_stars]{read_asset_stars()}}.}
}
\value{
A tibble with one row per year: \code{item}, \code{year}, \code{format}, \code{href},
\code{stars}.
}
\description{
Resolves the file hierarchy with \code{\link[=resolve_assets]{resolve_assets()}} and reads every year as a
\code{stars} object. Single band rasters get the attribute name \code{name}, so all
years are named alike and can be stacked.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
assets <- get_geo_admin_assets("ch.bafu.luftreinhaltung-feinstaub_pm2_5")
pm25 <- read_collection_rasters(assets, years = 2020:2024, formats = "tif", name = "pm2_5")
\dontshow{\}) # examplesIf}
}
