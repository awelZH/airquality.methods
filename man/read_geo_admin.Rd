% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-collections.R
\name{read_geo_admin}
\alias{read_geo_admin}
\title{Read several collections for the wanted years}
\usage{
read_geo_admin(
  collections,
  years = NULL,
  bbox = bbox_zh_lv95,
  specs = geo_admin_specs(),
  on_error = c("stop", "warn"),
  cache_dir = geo_admin_cache_dir()
)
}
\arguments{
\item{collections}{Collection ids.}

\item{years}{Wanted years (\code{NULL} = all available).}

\item{bbox}{Extent, by default the Canton of Zurich (LV95 or an \code{sf} object).}

\item{specs}{Collection specs, see \code{\link[=geo_admin_specs]{geo_admin_specs()}}.}

\item{on_error}{\code{"stop"} aborts, \code{"warn"} skips collections that fail.}

\item{cache_dir}{Cache directory.}
}
\value{
A tibble with one row per collection and year: \code{collection}, \code{label},
\code{year}, \code{format}, \code{res_x}, \code{res_y}, \code{grid}, \code{stars}, \code{item}, \code{href}, plus
the noloc columns for STATPOP.
}
\description{
For each collection the assets are queried, the file hierarchy is resolved and
only the available years are read; missing ones are reported. Every raster
keeps its original resolution — use \code{\link[=align_to_reference]{align_to_reference()}} or
\code{\link[=align_to_grid]{align_to_grid()}} to bring them onto a common grid.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
data <- read_geo_admin(
  c("ch.bfs.statistik-bevoelkerung_haushalte", "ch.bafu.luftreinhaltung-feinstaub_pm2_5"),
  years = 2020:2024
)
dplyr::select(data, label, year, format, grid)
\dontshow{\}) # examplesIf}
}
