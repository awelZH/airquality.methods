% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-statpop.R
\name{read_statpop_ha}
\alias{read_statpop_ha}
\title{Read the STATPOP hectare grid, one stars object per year}
\usage{
read_statpop_ha(
  assets,
  years = NULL,
  variables = "BBTOT",
  formats = c("parquet", "csv", "tif"),
  bbox = bbox_zh_lv95,
  ha_pattern = "_ha_",
  noloc_pattern = "_noloc_",
  noloc_formats = c("csv", "parquet"),
  noloc_variable = "BBTOT",
  year_col = "ERHJAHR",
  coords = c("E_KOORD", "N_KOORD"),
  anchor = c("lowerleft", "center"),
  correct_noloc = TRUE,
  crs = 2056,
  cache_dir = geo_admin_cache_dir(),
  ...
)
}
\arguments{
\item{assets}{Tibble from \code{\link[=get_geo_admin_assets]{get_geo_admin_assets()}}.}

\item{years}{Wanted years (\code{NULL} = all available).}

\item{variables}{Variables to read. Must contain \code{noloc_variable} when
correcting. Only \code{noloc_variable} is corrected.}

\item{formats}{Format preference for the hectare grid.}

\item{bbox}{Extent, by default the Canton of Zurich.}

\item{ha_pattern}{Regular expression for the hectare grid assets.}

\item{noloc_pattern}{Regular expression for the collector pixel assets.}

\item{noloc_formats}{Format preference for the collector pixel assets.}

\item{noloc_variable}{The inhabitant variable, in both datasets.}

\item{year_col}{Year column inside the collector pixel file.}

\item{coords, anchor}{Coordinate columns and their reference point.}

\item{correct_noloc}{Apply the correction?}

\item{crs}{Expected coordinate reference system.}

\item{cache_dir}{Cache directory.}

\item{...}{Further arguments passed to \code{\link[=read_asset_stars]{read_asset_stars()}}, \code{cellsize} for
instance.}
}
\value{
A tibble with one row per year: \code{item}, \code{year}, \code{format}, \code{href},
\code{stars}, \code{noloc_subtracted}, \code{noloc_unmatched}, \code{noloc}.
}
\description{
The file hierarchy is resolved with \code{\link[=resolve_assets]{resolve_assets()}} (pattern, EPSG, format
preference, years). Inhabitants that cannot be located are subtracted from
their collector pixels and carried alongside the raster:
\itemize{
\item \code{noloc_subtracted}: inhabitants removed within the extent
\item \code{noloc_unmatched}: inhabitants not removed, because the collector pixel lies
outside the bbox or its cell is empty
\item \code{noloc}: the per-pixel log
}
}
\details{
The invariant \verb{sum(noloc file) = noloc_subtracted + noloc_unmatched} holds.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
assets <- get_geo_admin_assets("ch.bfs.statistik-bevoelkerung_haushalte")
statpop <- read_statpop_ha(assets, years = 2015:2024)
dplyr::select(statpop, year, format, noloc_subtracted, noloc_unmatched)
\dontshow{\}) # examplesIf}
}
