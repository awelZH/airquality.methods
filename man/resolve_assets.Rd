% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-stac.R
\name{resolve_assets}
\alias{resolve_assets}
\title{Resolve the file hierarchy down to one asset per item}
\usage{
resolve_assets(
  assets,
  pattern = NULL,
  formats = c("tif", "parquet", "csv"),
  epsg = 2056,
  years = NULL
)
}
\arguments{
\item{assets}{Tibble from \code{\link[=get_geo_admin_assets]{get_geo_admin_assets()}}.}

\item{pattern}{Regular expression matched against the asset name.}

\item{formats}{Formats in decreasing preference.}

\item{epsg}{Expected EPSG code, or \code{NULL} to skip the crs stage.}

\item{years}{Wanted years (\code{NULL} = all available).}
}
\value{
A tibble with one row per item, sorted by year.
}
\description{
Applied in order:
\enumerate{
\item content: \code{pattern} as a regular expression on the asset name (\code{"_ha_"})
\item crs: \code{proj:epsg == epsg} (assets without the field are kept)
\item format: the first available format from \code{formats}, per item
\item uniqueness: prefer names carrying the EPSG code, otherwise abort with the
list of candidates
\item years: keep \code{years} only; unavailable years are reported
}
}
