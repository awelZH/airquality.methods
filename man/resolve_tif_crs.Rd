% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-raster.R
\name{resolve_tif_crs}
\alias{resolve_tif_crs}
\title{Check the crs of a GeoTIFF, accepting a file without EPSG code}
\usage{
resolve_tif_crs(x, asset, crs)
}
\arguments{
\item{x}{A \code{stars} or \code{stars_proxy} object.}

\item{asset}{The asset row, with \code{asset} and optionally \code{proj:epsg}.}

\item{crs}{Expected crs.}
}
\value{
\code{x}, with the expected crs.
}
\description{
A file in the expected crs is returned as it is. A file whose crs carries no
EPSG code (the BAFU PM10 maps 1998-2001: LV95 without EPSG code and datum
shift) is read in the expected crs, with a message, only if the STAC metadata
of the asset declare that crs (\code{proj:epsg}) and the projection of the file is
the same (\code{\link[=same_projection]{same_projection()}}). Everything else stops.
}
\keyword{internal}
