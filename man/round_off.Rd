% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{round_off}
\alias{round_off}
\title{Round half away from zero}
\usage{
round_off(x, digits = 0)
}
\arguments{
\item{x}{Numeric vector.}

\item{digits}{Number of decimal places.}
}
\value{
Numeric vector of the same length as \code{x}.
}
\description{
Commercial rounding ("kaufmännisches Runden"). Base R's \code{\link[=round]{round()}} rounds half
to even, so \code{round(0.5)} is \code{0} and \code{round(2.5)} is \code{2}. For reported figures
the expected behaviour is to round halves up in magnitude, symmetrically for
positive and negative values.
}
\details{
A decimal literal such as \code{1.005} is not exactly representable in binary (it
is stored as \verb{1.00499999...}), so a naive implementation rounds it down and
contradicts what the printed number suggests. A tolerance of
\code{sqrt(.Machine$double.eps)} absorbs that representation error, which is the
behaviour expected of commercial rounding.
}
\examples{
round(c(0.5, 1.5, 2.5))     # base R, half to even: 0 2 2
round_off(c(0.5, 1.5, 2.5)) # half away from zero:  1 2 3

round(2.675, 2)     # 2.67, because 2.675 is stored as 2.67499999...
round_off(2.675, 2) # 2.68

}
