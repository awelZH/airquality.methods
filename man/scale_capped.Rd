% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-capped.R
\name{scale_capped}
\alias{scale_capped}
\alias{scale_fill_capped}
\alias{scale_colour_capped}
\alias{scale_fill_capped_b}
\alias{scale_colour_capped_b}
\alias{scale_fill_capped_d}
\alias{scale_colour_capped_d}
\alias{scale_color_capped}
\alias{scale_color_capped_b}
\alias{scale_color_capped_d}
\title{Capped colour scale for ggplot2}
\usage{
scale_capped(
  aesthetics = "fill",
  type = c("continuous", "binned", "discrete"),
  palette = "Magma",
  limits = NULL,
  probs = NULL,
  direction = 1,
  values = NULL,
  space = "Lab",
  na.value = "grey80",
  oob = scales::oob_squish,
  cap = TRUE,
  include_cap_val = TRUE,
  cap_labels = TRUE,
  cap_min_dist = NULL,
  whiskers = TRUE,
  whisker_gap = grid::unit(2, "mm"),
  whisker_avoid_overlap = TRUE,
  whisker_stem = TRUE,
  whisker_width = 1,
  whisker_colour = NULL,
  whisker_linewidth = NULL,
  whisker_linetype = 1,
  whisker_labels = TRUE,
  guide = NULL,
  ...
)

scale_fill_capped(...)

scale_colour_capped(...)

scale_fill_capped_b(...)

scale_colour_capped_b(...)

scale_fill_capped_d(...)

scale_colour_capped_d(...)

scale_color_capped(...)

scale_color_capped_b(...)

scale_color_capped_d(...)
}
\arguments{
\item{aesthetics}{\code{"fill"}, \code{"colour"}/\code{"color"}, or a vector of these.}

\item{type}{\code{"continuous"}, \code{"binned"}, or \code{"discrete"}.}

\item{palette}{Palette name from \code{grDevices::hcl.pals()}, a colour vector,
or a function \code{f(n)}; see \code{\link[=.capped_palette]{.capped_palette()}}.}

\item{limits}{Numeric vector of length 2 (\code{NA} = open end) or \code{NULL}. For
\code{type = "discrete"}, the levels to show.}

\item{probs}{Quantiles for automatic limit calculation (see above).}

\item{direction}{1 or -1 (reverse the palette).}

\item{values}{Optional positions of the palette colours in [0, 1] (only
\code{type = "continuous"}; as in \verb{scale_*_gradientn(values = )}).}

\item{space}{Colour interpolation space, \code{"Lab"} or \code{"rgb"}.}

\item{na.value}{Colour for \code{NA} values.}

\item{oob}{Handling of values outside the limits. Default
\code{scales::oob_squish} — this is precisely what makes it a capped scale.
With \code{scales::oob_censor} (or \code{oob_keep}) such values become \code{NA}; then
there's no edge class, and cap symbols and whiskers are omitted
automatically — the scale behaves like an ordinary one.}

\item{cap, include_cap_val, cap_labels, cap_min_dist, whiskers, whisker_gap, whisker_avoid_overlap, whisker_stem, whisker_width, whisker_colour, whisker_linewidth, whisker_linetype, whisker_labels}{See \code{\link[=guide_colourbar_capped]{guide_colourbar_capped()}}.}

\item{guide}{Custom guide. Default \code{NULL} = the matching capped guide.
\code{"colourbar"}, \code{"coloursteps"}, \code{"legend"}, \code{"none"}, etc. switch back to
the standard ggplot2 guides.}

\item{...}{Further arguments for \code{\link[ggplot2:continuous_scale]{ggplot2::continuous_scale()}},
\code{\link[ggplot2:binned_scale]{ggplot2::binned_scale()}}, or \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}} — e.g. \code{name},
\code{breaks}, \code{n.breaks}, \code{labels}, \code{trans}/\code{transform}, \code{expand}, \code{right},
\code{show.limits}, \code{nice.breaks}, \code{rescaler}.}
}
\value{
A ggplot2 scale object.
}
\description{
Creates a continuous, binned, or discrete colour scale (\code{colour} and/or
\code{fill}) with a freely chosen palette. Values outside \code{limits} are not
turned into \code{NA} as usual, but squished to the edge (\code{scales::oob_squish});
the legend flags this with "≤"/"≥" and shows the true data extremes as
whiskers at a fixed distance outside the colour bar.
}
\section{Limits}{

\itemize{
\item \code{limits = c(0, 100)} — cap on both sides.
\item \code{limits = c(0, NA)} — cap only the low side; \code{NA} takes the data edge.
\item \code{probs = c(0.02, 0.98)} — limits as data quantiles (across all layers
mapped to this scale). \code{probs = 0.02} is shorthand for \code{c(0.02, 0.98)};
\code{probs = c(NA, 0.98)} caps only the high side.
\item \code{limits} and \code{probs} are mutually exclusive.
}

Cap symbols and whiskers only appear where data actually lie outside the
limits — and only when \code{oob} genuinely pulls those values to the edge (see
\code{oob} below).
}

\section{Scale types}{

\itemize{
\item \code{"continuous"} — smooth colour ramp, legend = colour bar.
\item \code{"binned"} — classed ramp, legend = colour steps. Class boundaries via
\verb{breaks = } or \verb{n.breaks = } (via \code{...}).
\item \code{"discrete"} — categorical scale. Capping/whiskers don't apply here and
are ignored; \code{limits} selects the factor levels as usual.
}
}

\examples{
library(ggplot2)
ggplot(faithfuld, aes(waiting, eruptions, fill = density)) +
  geom_raster() +
  scale_fill_capped(limits = c(0.005, 0.03), palette = "Viridis")

# the default palette is "Magma", i.e. viridis option "A"
ggplot(faithfuld, aes(waiting, eruptions, fill = density)) +
  geom_raster() +
  scale_fill_capped(limits = c(0.005, 0.03))

}
