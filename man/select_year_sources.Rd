% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-align.R
\name{select_year_sources}
\alias{select_year_sources}
\title{Choose the source year(s) for one target year}
\usage{
select_year_sources(rows, target_year, year_match, max_year_diff)
}
\arguments{
\item{rows}{Rows of one collection from \code{\link[=read_geo_admin]{read_geo_admin()}}.}

\item{target_year}{The year to produce.}

\item{year_match}{One of \code{"exact"}, \code{"nearest"}, \code{"linear"}.}

\item{max_year_diff}{Largest accepted distance in years.}
}
\value{
\code{NULL}, or a list with \code{rows} (1 or 2 rows), \code{weights} and \code{status}.
}
\description{
\itemize{
\item \code{"exact"}: the target year itself, nothing else (the default)
\item \code{"nearest"}: closest year within \code{max_year_diff}, ties go to the earlier one
\item \code{"linear"}: linear interpolation between the last year before and the first
after (both within \code{max_year_diff}). Outside the series the nearest year is
taken, as with \code{"nearest"}; it never extrapolates.
}
}
\keyword{internal}
