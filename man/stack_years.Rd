% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-cube.R
\name{stack_years}
\alias{stack_years}
\title{Stack years into cubes (x, y, year) without resampling}
\usage{
stack_years(x, by = intersect(c("collection", "label"), names(x)))
}
\arguments{
\item{x}{Tibble from \code{\link[=read_geo_admin]{read_geo_admin()}}, \code{\link[=align_to_reference]{align_to_reference()}} or
\code{\link[=align_to_grid]{align_to_grid()}}.}

\item{by}{Grouping columns in addition to the grid.}
}
\value{
A tibble with the \code{by} columns, \code{grid}, \code{years} and \code{cube}.
}
\description{
A \code{stars} cube has exactly one resolution per dimension, so one cube is
produced per group of identical grids: for raw data that is one per collection
and grid (several, if the resolution changes between years), for the result of
\code{\link[=align_to_reference]{align_to_reference()}} or \code{\link[=align_to_grid]{align_to_grid()}} usually exactly one.
}
