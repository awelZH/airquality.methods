% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-statpop.R
\name{subtract_noloc}
\alias{subtract_noloc}
\title{Subtract the inhabitants that cannot be located}
\usage{
subtract_noloc(
  x,
  noloc,
  variable = "BBTOT",
  coords = c("E_KOORD", "N_KOORD"),
  anchor = c("lowerleft", "center")
)
}
\arguments{
\item{x}{A two-dimensional \code{stars} object holding \code{variable}.}

\item{noloc}{Table of collector pixels with \code{coords} and \code{variable}.}

\item{variable}{Attribute to correct (inhabitants).}

\item{coords}{Coordinate columns in \code{noloc}.}

\item{anchor}{What the collector pixel coordinates refer to, see
\code{\link[=table_to_stars]{table_to_stars()}}.}
}
\value{
A list with \code{stars} (the corrected raster) and \code{noloc} (one log row
per collector pixel).
}
\description{
Every municipality has one collector pixel holding the inhabitants that cannot
be assigned to a location. This subtracts them from that raster cell and logs
every pixel, so the correction stays auditable instead of silently changing
the totals.
}
\details{
Status per collector pixel:
\itemize{
\item \code{"subtracted"}: fully removed
\item \code{"exceeds_cell"}: more to subtract than the cell holds; the cell went to 0
\item \code{"cell_missing"}: the cell is empty (\code{NA}) in the raster, nothing subtracted
\item \code{"outside_extent"}: the pixel lies outside the bbox, nothing subtracted
}
}
