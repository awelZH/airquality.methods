% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-admin-raster.R
\name{table_to_stars}
\alias{table_to_stars}
\title{Convert a regular point or cell table into a stars object}
\usage{
table_to_stars(
  data,
  variables = NULL,
  coords = c("E_KOORD", "N_KOORD"),
  cellsize = 100,
  anchor = c("lowerleft", "center"),
  crs = 2056,
  bbox = NULL
)
}
\arguments{
\item{data}{Table with coordinate columns and value columns.}

\item{variables}{Columns that become attributes (\code{NULL} = all numeric columns
except the coordinates).}

\item{coords}{Names of the coordinate columns (easting, northing).}

\item{cellsize}{Cell size in crs units.}

\item{anchor}{What the coordinates refer to: the lower left cell corner (as in
the BFS hectare grid) or the cell centre.}

\item{crs}{Coordinate reference system of the coordinates.}

\item{bbox}{Optional extent (see \code{\link[=as_bbox]{as_bbox()}}), snapped outwards onto the grid.
With a fixed \code{bbox} all results share the same grid and can be combined.}
}
\value{
A two-dimensional \code{stars} object with one attribute per variable.
}
\description{
Cells are filled directly by their index rather than rasterised from
geometries. That is exact, fast, and it verifies that the coordinates really
do lie on a regular grid instead of silently snapping them.
}
