% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_custom}
\alias{theme_custom}
\title{Theme for the recurring figure types}
\usage{
theme_custom(
  type = c("timeseries", "scatter", "flipped", "map", "default"),
  linecolor = "gray30"
)
}
\arguments{
\item{type}{One of \code{"timeseries"}, \code{"scatter"}, \code{"flipped"}, \code{"map"} or
\code{"default"}.}

\item{linecolor}{Colour of axis lines and ticks.}
}
\value{
A ggplot2 theme, to be added to a plot.
}
\description{
Builds on the currently active theme (\code{\link[ggplot2:theme_get]{ggplot2::theme_get()}}) and strips what
the respective figure type does not need, so the data stays the loudest thing
on the page.
}
\examples{
library(ggplot2)
ggplot(economics, aes(date, unemploy)) +
  geom_line() +
  theme_custom("timeseries")

}
