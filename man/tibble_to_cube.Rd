% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-cube.R
\name{tibble_to_cube}
\alias{tibble_to_cube}
\title{Put a long table back onto the grid of a cube}
\usage{
tibble_to_cube(data, template)
}
\arguments{
\item{data}{Table with \code{x}, \code{y} (cell centres), optionally \code{year}, and the
attribute columns.}

\item{template}{The cube whose grid is used.}
}
\value{
A \code{stars} object with every column except \code{x}, \code{y}, \code{year} as an
attribute.
}
\description{
The counterpart to \code{as_tibble(cube)}. Cells are matched by the coordinates of
the template, so rows may be missing (after a \code{\link[dplyr:filter]{dplyr::filter()}}, say) and
become \code{NA}. The crs, the grid and years-as-instants survive, which they would
not with \code{stars::st_as_stars(dims = ...)}. Without a \code{year} column the result
is two-dimensional, for instance after summarising per cell.
}
